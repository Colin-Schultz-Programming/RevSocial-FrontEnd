import { IState } from '.'
import { CommentActionTypes } from '../action-types/CommentActionTypes'

// const initialState: ICommentState = {

// }

export const commentReducer = (action: any) => {
    switch(action.type){
        default:
            console.log('commentReducer currently under construction')
    }
}